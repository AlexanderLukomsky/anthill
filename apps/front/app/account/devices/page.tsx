import { Metadata } from 'next';
import { projectName } from 'common/constants/main';

export const metadata: Metadata = {
  title: `${projectName} - account devices`,
};

const DevicesPage = () => <div>DevicesPage</div>;

export default DevicesPage;
